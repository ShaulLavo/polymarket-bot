<!DOCTYPE html>
<html lang="en" class="h-full bg-black">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <title>POLYMARKET TERMINAL v0.1</title>
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}></script>
  </head>
  <body class="h-full bg-black text-green-400 font-mono antialiased">
    <!-- Scanline overlay effect -->
    <div class="fixed inset-0 pointer-events-none z-50 bg-[repeating-linear-gradient(0deg,rgba(0,0,0,0.15),rgba(0,0,0,0.15)_1px,transparent_1px,transparent_2px)] opacity-30"></div>

    <!-- CRT glow effect -->
    <div class="fixed inset-0 pointer-events-none z-40 shadow-[inset_0_0_150px_rgba(0,255,0,0.05)]"></div>

    <%= @inner_content %>
  </body>
</html>
